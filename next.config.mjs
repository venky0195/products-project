/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['m.media-amazon.com'], // Add any other external image domains here
      },
};

export default nextConfig;
