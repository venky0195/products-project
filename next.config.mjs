/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api.slingacademy.com'], // Add any other external image domains here
      },
};

export default nextConfig;
